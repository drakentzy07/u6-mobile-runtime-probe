name: Runner Smoke

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Runner info
        run: |
          echo "RUNNER_OK"
          uname -a
          df -h /
          free -h
