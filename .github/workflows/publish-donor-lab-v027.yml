name: Publish HIGHFLY Donor Lab v2.7

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/publish-donor-lab-v027.yml"
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  pages: write
  id-token: write

concurrency:
  group: highfly-donor-lab-v027-pages
  cancel-in-progress: true

jobs:
  stage:
    name: Merge current Pages + DONOR Lab 2.7
    runs-on: ubuntu-latest
    steps:
      - name: Download current combined Pages site
        uses: actions/download-artifact@v4
        with:
          name: github-pages
          path: current-pages
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 35921775211

      - name: Download verified DONOR Lab 2.7 WebGL
        uses: actions/download-artifact@v4
        with:
          name: github-pages
          path: donor-v27-pages
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 35925478257

      - name: Assemble combined site
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p site donor-v27-extracted

          test -f current-pages/artifact.tar
          tar -xf current-pages/artifact.tar -C site

          test -f site/index.html
          test -f site/donor-lab-v25/index.html
          test -f site/donor-lab-v26/index.html

          test -f donor-v27-pages/artifact.tar
          tar -xf donor-v27-pages/artifact.tar -C donor-v27-extracted
          test -f donor-v27-extracted/index.html
          grep -q "v=0270" donor-v27-extracted/index.html
          grep -q "highfly-desktop-fit" donor-v27-extracted/index.html

          rm -rf site/donor-lab-v27
          mkdir -p site/donor-lab-v27
          cp -a donor-v27-extracted/. site/donor-lab-v27/

          test -f site/donor-lab-v27/index.html
          test -f site/donor-lab-v27/Build/WebGL.loader.js
          test -f site/donor-lab-v27/Build/WebGL.wasm
          test -f site/donor-lab-v27/Build/WebGL.data

          echo "HIGHFLY DONOR LAB 2.7 • VERIFIED BUILD RUN 35925478257 • KAYKIT HUNTER CORE" > site/donor-lab-v27/published-version.txt
          touch site/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload combined Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    name: Deploy current Pages + DONOR Lab 2.7
    needs: stage
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
